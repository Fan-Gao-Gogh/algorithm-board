题目链接：https://codeforces.com/contest/2226/problem/A

题目大意：
给定一个数组a，你可以执行任意次以下操作，将数组清空：

选择一组下标，必须为非递减子序列，将其移除，花费代价为乘积。

计算清空数组的最小总代价，对676767677取模。



贪心思路：当a，b都不等于1时，分开移除总是比合起来移除消费小，即：a*b>=a+b;

从后往前遍历数组，x>1,直接相加。x=1,检测后一位是否存在，若存在，则不管。若不存在，则将其作为单独一项移除，加1。
