题目链接：https://codeforces.com/contest/2226/problem/B

题目大意：给定一个长度为n的排列p（从1到n每个数字必须出现一次），求其中好子数组（连续的数组）的数量。

好数组：当且仅当数组中最大值与最小值的差，等于数组所有元素的最大公约数（GCD）。

eg:空数组不为好数组

贪心思路：Max-min=gcd。gcd为最大公约数，两边同除gcd，(Max-min)/gcd=1,即Max与min的差值应该为1，那么说明子数组最多只能有两个数字，最大值-最小值才有机会等于1。

所以只需要遍历相邻两个元素是否满足条件即可
